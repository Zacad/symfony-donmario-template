<?php

declare(strict_types=1);

namespace App\Module\CollectionChecking\Application\Collect;

use App\Platform\Authorization\PolicyContext;

/** Disposable collection-validation operations only; nested production calls reauthorize. */
final readonly class CollectPolicy
{
    public function __invoke(CollectCommand $message, PolicyContext $context): bool
    {
        return true;
    }
}
