<?php

declare(strict_types=1);

namespace App\Module\CollectionChecking\Application\Record;

use App\Platform\Authorization\PolicyContext;

/** Disposable observation writes for the void-result transaction probe. */
final readonly class RecordPolicy
{
    public function __invoke(RecordCommand $message, PolicyContext $context): bool
    {
        return true;
    }
}
