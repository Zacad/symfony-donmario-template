<?php

declare(strict_types=1);

namespace App\Module\NativeObserving\Application\Probe;

use App\Platform\Authorization\PolicyContext;

/** Disposable probe must reach the forbidden query-to-event dispatch. */
final readonly class ProbePolicy
{
    public function __invoke(ProbeQuery $message, PolicyContext $context): bool
    {
        return true;
    }
}
