<?php

declare(strict_types=1);

namespace App\Module\NativeObserving\Application\Publish;

use App\Platform\Authorization\PolicyContext;

/** Disposable event producer used only by the native-delivery verification project. */
final readonly class PublishPolicy
{
    public function __invoke(PublishCommand $message, PolicyContext $context): bool
    {
        return true;
    }
}
